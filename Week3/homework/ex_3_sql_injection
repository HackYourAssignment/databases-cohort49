function getPopulation(Country, name, code, cb) {
  // Assuming that connection to the database is established and stored as conn
  conn.query(
    `SELECT Population FROM ?? WHERE Name = ? AND CountryCode = ?`,
    [Country, name, code],
    function (err, result) {
      if (err) return cb(err);
      if (result.length == 0) return cb(new Error("Not found"));
      cb(null, result[0].Population);
    }
  );
}

/* 

Example of SQL Injection Values:
To exploit the SQL injection vulnerability in the original code, we could pass the following values:

- name: ' OR '1'='1
- code: ' OR '1'='1'

This will generate a query that returns all records in the table because '1'='1' is always true:

SELECT Population FROM ${Country} WHERE Name = '' OR '1'='1' AND code = '' OR '1'='1'

*/
